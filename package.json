{
  "name": "pixi-mobile-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "music:manifest": "node scripts/generate-music-manifest.mjs",
    "dev": "npm run music:manifest && vite",
    "build": "npm run music:manifest && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.16.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.0.0"
  }
}
